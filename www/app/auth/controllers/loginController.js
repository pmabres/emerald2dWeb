'use strict';
angular.module('authModule').controller('loginController', ['$scope', '$state', 'authService', 'backEndConstant','loginModel', function ($scope, $state, authService, backEndConstant,loginModel) {
    //TODO: Remove this controller because this functionality is now performed by login directive
}]);
